# ============================================================
# Digital Kisan Admin Panel — Environment Variables Template
# Copy this file to .env and fill in your real values.
# NEVER commit the actual .env file — it contains secrets.
# Vite exposes only VITE_ prefixed variables to the browser.
# ============================================================

# Backend API base URL (no trailing slash)
VITE_API_URL=https://your-render-app.onrender.com/api/v1
